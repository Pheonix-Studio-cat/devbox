{
  "name": "devbox-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Devbox's tools exposed as a remote MCP server on Cloudflare Workers.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.22.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.129.0"
  }
}
